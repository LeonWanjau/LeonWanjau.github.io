import {makeStyles} from '@material-ui/core/styles'

const topItemHeight='10%'
const bottomItemHeight='20%'
const spacerHeight='10%'
const drawerItems='3'

const useStyles=makeStyles(theme=>({
    
}))

export default useStyles