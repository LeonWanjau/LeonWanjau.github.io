//import {makeStyles} from '@material-ui/core/styles'
import CircularProgress from '@material-ui/core/CircularProgress'

const MuiLoader=({size})=>{

    return (
            <CircularProgress size={size}/>
    )
}

export default MuiLoader